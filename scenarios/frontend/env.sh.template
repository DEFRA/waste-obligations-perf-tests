#!/bin/sh
# Copy to env.sh and fill in your values. The entrypoint sources it.

export CI=false

# Selects the frontend base URL (rwd-{env}.azure.defra.cloud).
# Recognised values: dev, tst1, perf-test. Set EPR_BASE_URL below to override
# directly, e.g. for a CDP review environment.
export ENVIRONMENT=perf-test
# export EPR_BASE_URL=https://rwd-perf-test.azure.defra.cloud

# When invoking this frontend entrypoint directly: all, lighthouse, or
# browser-load. The root entrypoint also accepts k6.
export PROFILE=all

# DefraID credentials (same vars as the journey-tests). Browser-load needs
# only account types included in the configured user mix; the standalone
# Lighthouse profile audits both account types.
export EPR_USER_EMAIL=
export EPR_USER_PASSWORD=
export EPR_CSO_USER_EMAIL=
export EPR_CSO_USER_PASSWORD=

# The Azure stub creates exact direct-producer and compliance-scheme allocations
# for the browser mix below. With the defaults, this is 10 DP and 30 CSO users.
export EPR_AZURE_STUB_BASE_URL=
# export LOAD_TEST_USER_COUNT=40
# export LOAD_TEST_CSO_PERCENTAGE=75
# Number of complete browser journeys each allocated virtual user performs.
# Defaults to 1; users keep their allocated organisation and correlation header.
# export LOAD_TEST_USER_ITERATIONS=1
# Maximum randomized delay before each virtual user starts its first journey.
# Milliseconds; defaults to 0. For example, 30000 spreads starts over 30 seconds.
# export LOAD_TEST_USER_START_JITTER_MS=0
# export EPR_LOAD_TEST_RUN_ID=
# Seed organisations used to enter the authenticated UI flows. Use the seeded
# direct producer Organisations.ExternalId and compliance-scheme
# ComplianceSchemes.ExternalId respectively. The values are only required for
# account types included in the configured mix. Lighthouse resets declarations
# for every audited seeded organisation; browser load-test users submit and
# cancel declarations on their generated organisation allocations.
# EPR_ORG_ID=e2316c5e-d434-41da-8274-494dc0762d20
# WASTE_OBLIGATION_CSO_ORG_ID=d93376e3-0681-46be-aeb4-7450a2e784d8
export EPR_ORG_ID=
export WASTE_OBLIGATION_CSO_ORG_ID=

# Backend basic-auth credentials and the submitter identity stamped on
# declarations cancelled by Lighthouse and the browser load test. Same vars
# as the journey-tests.
export WASTE_OBLIGATION_USERNAME=
export WASTE_OBLIGATION_PASSWORD=
export WASTE_OBLIGATION_SUBMITTER_ID=
export WASTE_OBLIGATION_SUBMITTER_EMAIL=

# Override for the backend host. Defaults to
# waste-obligations.{env}.cdp-int.defra.cloud derived from ENVIRONMENT. Use
# the ephemeral protected gateway URL for local runs.
# export EPR_BACKEND_BASE_URL=https://ephemeral-protected.api.perf-test.cdp-int.defra.cloud/waste-obligations

# API gateway key (sent as x-api-key alongside basic auth). Required when
# pointing EPR_BACKEND_BASE_URL at the ephemeral protected URL.
# export X_API_KEY=

# Lighthouse performance score below which the run exits non-zero (0-1 range).
# export PERFORMANCE_FLOOR=0.5

# HTTP proxy (format: http://host:port). Forwarded to Chromium.
# export HTTP_PROXY=http://proxy.example.com:8080
